# ============================================
# Настройки сервера
# ============================================

# Порт сервера (по умолчанию 3001)
PORT=3001

# Хост сервера
# 0.0.0.0 - доступ из локальной сети (для мобильных устройств)
# 127.0.0.1 - только локальный доступ
HOST=0.0.0.0

# ============================================
# HTTPS настройки
# ============================================

# Включить HTTPS (true/false)
# Для работы микрофона на Safari iOS требуется HTTPS
HTTPS_ENABLED=false

# Путь к SSL сертификату (если HTTPS_ENABLED=true)
HTTPS_CERT_PATH=./certs/localhost+3.pem

# Путь к приватному ключу (если HTTPS_ENABLED=true)
HTTPS_KEY_PATH=./certs/localhost+3-key.pem

# ============================================
# Система распознавания речи
# ============================================

# Выбор системы распознавания: 'openai', 'local', 'vosk'
# По умолчанию: 'openai'
SPEECH_RECOGNITION_SYSTEM=openai

# Автоматическое переключение на резервную систему при ошибках
# true - включено (рекомендуется)
# false - отключено
AUTO_FALLBACK_TO_LOCAL=true

# ============================================
# OpenAI API (если SPEECH_RECOGNITION_SYSTEM=openai)
# ============================================

# API ключ OpenAI
# Получить можно на: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_api_key_here

# ============================================
# Vosk (если SPEECH_RECOGNITION_SYSTEM=vosk)
# ============================================

# Путь к модели Vosk
# Скачать модели можно на: https://alphacephei.com/vosk/models
# Для русского языка рекомендуется: vosk-model-ru-0.22
VOSK_MODEL_PATH=./models/vosk-model-ru-0.22

# ============================================
# База данных
# ============================================

# Путь к файлу базы данных SQLite (опционально)
# По умолчанию: ./database.sqlite
# DATABASE_PATH=./database.sqlite

# ============================================
# Примечания
# ============================================
#
# 1. Скопируйте этот файл в .env:
#    cp env.example .env
#    (на Windows: copy env.example .env)
#
# 2. Заполните необходимые переменные окружения
#
# 3. НЕ коммитьте .env файл в репозиторий!
#
# 4. Для локального Whisper (SPEECH_RECOGNITION_SYSTEM=local):
#    - Не требует API ключ
#    - Модель загружается автоматически при первом использовании
#    - Может занять несколько минут при первом запуске
#
# 5. Для Vosk (SPEECH_RECOGNITION_SYSTEM=vosk):
#    - Не требует API ключ
#    - Требует установки пакета vosk (npm install vosk)
#    - Требует скачивания модели и указания пути в VOSK_MODEL_PATH
#    - На Windows требуется Visual Studio с компонентами C++
#
# 6. Для OpenAI API (SPEECH_RECOGNITION_SYSTEM=openai):
#    - Требует API ключ
#    - Работает через интернет
#    - Рекомендуется для продакшена

