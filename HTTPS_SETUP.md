# Настройка HTTPS для локальной разработки

Для работы микрофона на Safari (iOS) и некоторых других браузерах требуется HTTPS. Эта инструкция поможет настроить HTTPS для локального тестирования.

## Вариант 1: Использование mkcert (Рекомендуется)

`mkcert` создает локальные сертификаты, которые доверяются браузерами автоматически.

### Шаг 1: Установка mkcert

**Windows:**

**Вариант 1: Скачать готовый исполняемый файл (Рекомендуется):**
1. Перейдите на https://github.com/FiloSottile/mkcert/releases/latest
2. Скачайте файл `mkcert-v1.4.4-windows-amd64.exe` (или последнюю версию)
3. Переименуйте файл в `mkcert.exe`
4. Переместите в папку, которая есть в PATH (например, `C:\Windows\System32` или создайте папку `C:\tools` и добавьте её в PATH)
5. Или просто поместите в любую папку и используйте полный путь

**Вариант 2: Через Scoop (если установлен):**
```powershell
scoop bucket add extras
scoop install mkcert
```

**Вариант 3: Через Chocolatey (если установлен):**
```powershell
choco install mkcert
```

**Вариант 4: Установить Chocolatey (если не установлен):**
```powershell
# Запустите PowerShell от имени администратора
Set-ExecutionPolicy Bypass -Scope Process -Force; [System.Net.ServicePointManager]::SecurityProtocol = [System.Net.ServicePointManager]::SecurityProtocol -bor 3072; iex ((New-Object System.Net.WebClient).DownloadString('https://community.chocolatey.org/install.ps1'))

# Затем установите mkcert
choco install mkcert
```

**macOS:**
```bash
brew install mkcert
```

**Linux:**
```bash
# Ubuntu/Debian
sudo apt install libnss3-tools
wget -O mkcert https://github.com/FiloSottile/mkcert/releases/latest/download/mkcert-v1.4.4-linux-amd64
chmod +x mkcert
sudo mv mkcert /usr/local/bin/
```

### Шаг 2: Создание локального CA (Certificate Authority)

```bash
mkcert -install
```

Это создаст локальный CA и добавит его в доверенные корневые сертификаты вашей системы.

**На Windows может потребоваться запуск PowerShell от имени администратора.**

### 🚀 Быстрая настройка (Windows) - Автоматический скрипт

В корне проекта есть скрипт `setup-https.ps1`, который автоматически:
- Проверяет наличие mkcert
- Устанавливает локальный CA
- Создает сертификаты для всех локальных IP адресов
- Обновляет файл `server/.env`

**Запуск:**
```powershell
.\setup-https.ps1
```

**Примечание:** Если скрипт не запускается из-за политики выполнения, выполните:
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

**Важно:** Перед запуском скрипта убедитесь, что mkcert установлен и доступен в PATH (см. Шаг 1).

### Шаг 3: Создание сертификатов

Создайте папку для сертификатов:
```bash
mkdir -p server/certs
```

Создайте сертификаты для localhost и локального IP:
```bash
cd server/certs

# Для localhost
mkcert localhost 127.0.0.1

# Для локальной сети (замените на ваш IP)
# Сначала узнайте ваш IP:
# Windows: ipconfig | findstr IPv4
# Linux/Mac: ifconfig | grep "inet "

# Затем создайте сертификат:
mkcert localhost 127.0.0.1 192.168.1.100 ::1
```

Это создаст два файла:
- `localhost+2.pem` (сертификат)
- `localhost+2-key.pem` (приватный ключ)

### Шаг 4: Настройка сервера

Сервер уже настроен для работы с HTTPS. Убедитесь, что в `server/.env` указаны пути к сертификатам:

```env
HTTPS_ENABLED=true
HTTPS_CERT_PATH=./certs/localhost+2.pem
HTTPS_KEY_PATH=./certs/localhost+2-key.pem
```

### Шаг 5: Настройка Vite (фронтенд)

Vite также настроен для работы с HTTPS. Убедитесь, что в `client/vite.config.js` включен HTTPS режим.

### Шаг 6: Запуск приложения

```bash
npm run dev
```

Теперь приложение будет доступно по адресу:
- `https://localhost:5173` (фронтенд)
- `https://localhost:3001` (бэкенд)
- `https://192.168.1.100:5173` (с мобильного устройства)

## Вариант 2: Самоподписанный сертификат (без mkcert)

Если не хотите устанавливать mkcert, можно создать самоподписанный сертификат вручную.

### Создание сертификата с помощью OpenSSL

**Windows:**
```powershell
# Установите OpenSSL через Chocolatey или скачайте с https://slproweb.com/products/Win32OpenSSL.html
openssl req -x509 -newkey rsa:4096 -nodes -keyout server/certs/key.pem -out server/certs/cert.pem -days 365 -subj "/CN=localhost"
```

**Linux/Mac:**
```bash
openssl req -x509 -newkey rsa:4096 -nodes -keyout server/certs/key.pem -out server/certs/cert.pem -days 365 -subj "/CN=localhost"
```

### Настройка в .env

```env
HTTPS_ENABLED=true
HTTPS_CERT_PATH=./certs/cert.pem
HTTPS_KEY_PATH=./certs/key.pem
```

**⚠️ Важно:** Браузеры будут показывать предупреждение о небезопасном соединении. Нужно будет:
1. Нажать "Дополнительно" или "Advanced"
2. Выбрать "Продолжить" или "Proceed to localhost"

## Вариант 3: Использование ngrok (для внешнего доступа)

Если нужно протестировать на реальном устройстве через интернет:

1. Установите ngrok: https://ngrok.com/
2. Запустите приложение локально
3. Запустите ngrok:
   ```bash
   ngrok http 5173
   ```
4. Используйте HTTPS URL, который предоставит ngrok

## Настройка для мобильных устройств

### iOS Safari

1. Убедитесь, что используете HTTPS
2. Если используете самоподписанный сертификат:
   - Откройте сайт на iPhone/iPad
   - Нажмите "Дополнительно" → "Посетить сайт"
   - Сертификат будет добавлен в доверенные

### Android Chrome

1. Убедитесь, что используете HTTPS
2. Если используете самоподписанный сертификат:
   - Откройте сайт
   - Нажмите "Дополнительно" → "Продолжить"
   - Сертификат будет принят

## Проверка работы

1. Откройте `https://localhost:5173` в браузере
2. Проверьте, что нет предупреждений о безопасности (при использовании mkcert)
3. Попробуйте записать аудио - микрофон должен работать

## Решение проблем

### Ошибка "ERR_CERT_AUTHORITY_INVALID"

- Если используете mkcert: убедитесь, что выполнили `mkcert -install`
- Если используете самоподписанный сертификат: это нормально, нужно принять предупреждение

### Ошибка "ERR_CERT_COMMON_NAME_INVALID"

- Убедитесь, что в сертификате указан правильный домен/IP
- Пересоздайте сертификат с правильными параметрами

### Порт занят

- Измените порты в `server/.env` и `client/vite.config.js`
- Или остановите другие приложения, использующие эти порты

## Отключение HTTPS

Если нужно вернуться к HTTP, установите в `server/.env`:
```env
HTTPS_ENABLED=false
```

И в `client/vite.config.js` отключите HTTPS режим.

