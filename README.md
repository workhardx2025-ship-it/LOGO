# 🎤 Приложение для логопеда

Веб-приложение для логопедов с распознаванием голоса на основе Whisper API.

## Возможности

- 📝 Регистрация учеников (ФИО)
- 📚 Создание уроков с набором слов для произношения
- 🎤 Распознавание речи в реальном времени с помощью Whisper API
- 🖼️ Отображение картинок для каждого слова
- ✅ Проверка правильности произношения
- 📊 Отслеживание прогресса учеников с графиками
- 🎉 Система поощрения при правильном произношении

## Технологии

- **Backend**: Node.js + Express + SQLite
- **Frontend**: React + Vite
- **Распознавание речи**: OpenAI Whisper API
- **Графики**: Recharts

## Установка

1. Установите зависимости:
```bash
npm run install-all
```

2. Настройте переменные окружения:
```bash
cd server
cp .env.example .env
```

Откройте `server/.env` и настройте:

**По умолчанию используется OpenAI API:**
```
OPENAI_API_KEY=your_api_key_here
SPEECH_RECOGNITION_SYSTEM=openai
AUTO_FALLBACK_TO_LOCAL=true
```

**Для использования локального Whisper (не требует API ключ):**
```
SPEECH_RECOGNITION_SYSTEM=local
```

**Для использования Vosk (не требует API ключ, но требует модель):**
```
SPEECH_RECOGNITION_SYSTEM=vosk
VOSK_MODEL_PATH=./models/vosk-model-ru-0.22
```

**Для включения HTTPS (для работы микрофона на Safari iOS):**
```
HTTPS_ENABLED=true
HTTPS_CERT_PATH=./certs/localhost+2.pem
HTTPS_KEY_PATH=./certs/localhost+2-key.pem
```

См. подробные инструкции в `HTTPS_SETUP.md`

**Примечания:**
- Если `SPEECH_RECOGNITION_SYSTEM` не указан, по умолчанию используется `openai`.
- `AUTO_FALLBACK_TO_LOCAL=true` включает автоматическое переключение на локальный Whisper или Vosk при ошибках подключения к OpenAI API (например, при проблемах с VPN). Установите `AUTO_FALLBACK_TO_LOCAL=false`, чтобы отключить эту функцию.
- Для использования Vosk необходимо:
  1. Установить пакет `vosk` (требует компиляции нативных модулей, **на Windows обязательно нужен Visual Studio с компонентами C++**)
  2. Скачать модель с https://alphacephei.com/vosk/models (например, `vosk-model-ru-0.22` для русского языка)
  3. Распаковать модель в директорию `server/models/` или указать путь в `VOSK_MODEL_PATH`
  
  **⚠️ Если установка vosk не удалась на Windows:** Установите Visual Studio Community с рабочей нагрузкой "Desktop development with C++" или используйте альтернативные методы (OpenAI API или локальный Whisper). Подробнее см. `VOSK_SETUP.md`

При первом использовании локального Whisper модель будет автоматически загружена (может занять несколько минут).

3. Запустите приложение:
```bash
npm run dev
```

Приложение будет доступно:
- Frontend: http://127.0.0.1:5173 (или http://localhost:5173)
- Backend: http://127.0.0.1:3001 (или http://localhost:3001)

**Примечание:** Приложение настроено на использование `127.0.0.1` для лучшей совместимости с VPN. Если у вас проблемы с VPN, см. файл `VPN_TROUBLESHOOTING.md`.

## Использование

1. **Регистрация ученика**: На главной странице нажмите "Добавить ученика" и введите ФИО
2. **Создание урока**: Выберите ученика, нажмите "Создать урок", добавьте название, описание, проблемные звуки и список слов
3. **Проведение урока**: Нажмите "Начать урок" для выбранного урока
4. **Во время урока**: 
   - На экране отображается картинка и слово
   - Нажмите "Начать запись" и произнесите слово
   - При правильном произношении появится окно поощрения
   - Автоматический переход к следующему слову
5. **Просмотр прогресса**: На странице ученика отображаются графики и статистика всех занятий

## Структура проекта

```
├── server/          # Backend (Express API)
│   ├── routes/      # API маршруты
│   ├── db.js        # База данных SQLite
│   └── index.js     # Главный файл сервера
├── client/          # Frontend (React)
│   └── src/
│       ├── components/  # React компоненты
│       └── api.js      # API клиент
└── package.json
```

## Примечания

- Для работы распознавания речи необходим OpenAI API ключ
- Картинки для слов можно добавлять через путь к файлу или URL
- Все данные сохраняются в локальной базе данных SQLite
- Приложение работает в браузере и требует разрешения на использование микрофона

