# Тестирование приложения на мобильном устройстве

## Подготовка

### 1. Узнайте IP адрес вашего компьютера в локальной сети

**Windows (PowerShell):**
```powershell
ipconfig | findstr IPv4
```

**Windows (CMD):**
```cmd
ipconfig
```
Ищите строку "IPv4 Address" в разделе вашего WiFi адаптера.

**Linux/Mac:**
```bash
ifconfig | grep "inet "
```
или
```bash
ip addr show
```

Обычно это адрес вида `192.168.x.x` или `10.0.x.x`.

### 2. Настройте сервер для доступа из сети

Сервер уже настроен на прослушивание всех интерфейсов (`0.0.0.0`), что позволяет подключаться как локально, так и из сети.

Если вы используете VPN и хотите ограничить доступ только локальным, установите в `server/.env`:
```
HOST=127.0.0.1
```

Для доступа из сети оставьте по умолчанию или установите:
```
HOST=0.0.0.0
```

### 3. Настройте фронтенд

Фронтенд также настроен на `0.0.0.0` по умолчанию.

### 4. Проверьте брандмауэр

**Windows:**
1. Откройте "Брандмауэр Защитника Windows"
2. Нажмите "Дополнительные параметры"
3. Создайте правило для входящих подключений:
   - Тип: Для порта
   - Протокол: TCP
   - Порт: 3001 и 5173
   - Действие: Разрешить подключение

Или временно отключите брандмауэр для тестирования (не рекомендуется для продакшена).

### 5. Запустите сервер и фронтенд

```bash
# В корне проекта
npm run dev
```

Сервер выведет в консоль IP адреса, по которым можно подключиться:
```
📱 Для доступа с мобильного устройства в той же WiFi сети:
   http://192.168.1.100:3001
```

### 6. Откройте приложение на смартфоне

1. Убедитесь, что смартфон подключен к той же WiFi сети
2. Откройте браузер на смартфоне
3. **ВАЖНО:** Введите адрес с явным указанием протокола `http://` (не `https://`), например:
   ```
   http://192.168.1.100:5173
   ```
   
   ⚠️ **Не используйте `https://`** - приложение работает по HTTP для локального тестирования.
   
4. Если браузер автоматически добавляет `https://`, удалите `s` из `https` или введите адрес заново с `http://`

## Важно

- ✅ Оба устройства (компьютер и смартфон) должны быть в одной WiFi сети
- ✅ Брандмауэр должен разрешать подключения на порты 3001 и 5173
- ✅ IP адрес компьютера может измениться при переподключении к WiFi
- ⚠️ Если используете VPN, может потребоваться установить `HOST=127.0.0.1` для локального доступа

## Решение проблем

### Не могу подключиться с телефона

1. **Проверьте IP адрес** - он мог измениться
2. **Проверьте брандмауэр** - убедитесь, что порты открыты
3. **Проверьте WiFi** - оба устройства должны быть в одной сети
4. **Попробуйте отключить VPN** на компьютере
5. **Проверьте, что сервер запущен** и слушает на `0.0.0.0`

### Ошибки CORS

Если возникают ошибки CORS, убедитесь, что в `server/index.js` включен CORS:
```javascript
app.use(cors());
```

### Ошибка "этот сайт не может обеспечить безопасное подключение"

Эта ошибка возникает, когда браузер пытается использовать HTTPS вместо HTTP.

**Решение:**
1. **Убедитесь, что используете `http://` (не `https://`)** в адресной строке
2. Если браузер автоматически добавляет `https://`, введите адрес заново с явным указанием `http://`
3. Попробуйте другой браузер (Chrome, Firefox, Edge)
4. Очистите кеш браузера и попробуйте снова

**Пример правильного адреса:**
```
http://192.168.1.100:5173
```

**Неправильно:**
```
https://192.168.1.100:5173  ❌
192.168.1.100:5173          ❌ (браузер может автоматически добавить https)
```

### Проблемы с микрофоном на мобильном

На мобильных устройствах браузеры могут требовать HTTPS для доступа к микрофону. Для локального тестирования:
- **Chrome на Android** - обычно работает с HTTP в локальной сети
- **Safari на iOS** - требует HTTPS даже в локальной сети (может не работать с HTTP)
- **Firefox на Android** - обычно работает с HTTP в локальной сети

**Для iOS Safari:** Если микрофон не работает, это ограничение браузера. Для полноценного тестирования на iOS потребуется HTTPS сертификат.

**Для продакшена обязательно используйте HTTPS.**

